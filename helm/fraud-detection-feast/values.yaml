namespace: fraud-detection-ml
storageClassName: gp3-csi

postgres:
  password: changeme
  database: feast_db
  user: feast_user
  storageSize: 10Gi

minio:
  rootUser: minio
  rootPassword: minio123
  storageSize: 10Gi
  bucketName: feast-registry

featureStore:
  gitUrl: https://github.com/mouachan/fraud-detection-ml
  gitRef: main
  subPath: feature_repo

mlflow:
  trackingUri: https://mlflow.redhat-ods-applications.svc.cluster.local:8443

feastInit:
  enabled: false
